{
  "name": "geo-bounty-uploader",
  "version": "1.0.0",
  "description": "Generic SDK to upload any bounty dataset to the Geo Browser knowledge graph",
  "type": "module",
  "scripts": {
    "upload": "tsx --env-file=.env sdk/upload.ts",
    "delete": "tsx --env-file=.env sdk/delete.ts",
    "query": "tsx --env-file=.env query.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@geoprotocol/geo-sdk": "^0.13.1",
    "dotenv": "^17.2.4"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}